services:
  ai-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slik-ai-manager
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
